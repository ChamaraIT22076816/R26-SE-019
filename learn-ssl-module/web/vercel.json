{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "vite",
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "headers": [
    {
      "// note": "The hand model and the reference recordings are content-addressed by name and never change in place, so they can be cached hard. This matters: the model alone is 7.8 MB and every participant downloads it once.",
      "source": "/(models|references|wasm)/(.*)",
      "headers": [{ "key": "Cache-Control", "value": "public, max-age=31536000, immutable" }]
    }
  ]
}
